server:
  port: 8080
  servlet:
    context-path: /project

spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/big_event?useUnicode=ture?characterEncoding=utf-8&useSSL=false&serverTimezone=UTC
    username: root
    password: root
